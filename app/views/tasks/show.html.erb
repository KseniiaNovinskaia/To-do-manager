<h1><%= @task.title %></h1>

<p> <%= @task.details %></p>
<p><input name="completed" type="checkbox" <%= 'checked' if @task.completed == true %>/> <%= @task.completed ? "This task is completed": "This task is not completed" %></p>

<%= link_to "Back to list", "/tasks" %>

<%= link_to "Edit this task", update_path %>
